import fs from 'node:fs/promises';
import path from 'node:path';
const dir=path.resolve(import.meta.dirname,'../assets/fonts');await fs.mkdir(dir,{recursive:true});
const families=['Manrope','Cormorant Garamond','Italiana','DM Sans','Inter','Space Grotesk','Libre Baskerville','Syne','Barlow Condensed','Archivo','Playfair Display','Bodoni Moda','Plus Jakarta Sans','Lora','Fraunces','IBM Plex Sans','IBM Plex Serif','Space Mono','Sora','IBM Plex Mono'];
for(const family of families){try{const response=await fetch('https://fonts.googleapis.com/css2?family='+encodeURIComponent(family)+':wght@400..700&display=swap',{headers:{'User-Agent':'Mozilla/5.0 Chrome/120.0.0.0 Safari/537.36'}});let css=await response.text();if(!response.ok){const alt=await fetch('https://fonts.googleapis.com/css2?family='+encodeURIComponent(family)+'&display=swap',{headers:{'User-Agent':'Mozilla/5.0 Chrome/120.0.0.0 Safari/537.36'}});css=await alt.text();}const urls=[...css.matchAll(/url\((https:[^)]+)\)/g)];if(!urls.length)throw Error('No font URL');const res=await fetch(urls.at(-1)[1]);if(!res.ok)throw Error(res.status);await fs.writeFile(path.join(dir,family.toLowerCase().replaceAll(' ','-')+'.woff2'),Buffer.from(await res.arrayBuffer()));console.log(family+' saved');}catch(e){console.log(family+': '+e.message);process.exitCode=1;}}
